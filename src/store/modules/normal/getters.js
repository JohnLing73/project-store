export default {
    theme(state) {
        return state.colorTheme;
    },
    slideData(state) {
        return state.wrapData;
    },
    profileFig(state) {
        return state.profileIcon;
    },
    cartFig(state) {
        return state.cartIcon;
    }
}